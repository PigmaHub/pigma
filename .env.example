# SCP deployment configuration
# Rename this file to `.env` and set values, or create `.env` yourself.
# Values in .env override positional arguments when running the script.

# Remote SSH username
SCP_USER=
# Remote SSH host (IP or domain)
SCP_HOST=
# Remote path where the dist folder will be uploaded
SCP_REMOTE_PATH=
# SSH port (default 22)
SCP_PORT=22
# SSH private key path (optional). Leave blank to use default key.
SSH_KEY_PATH=
# Local directory to upload (default apps/design/dist)
LOCAL_DIR=apps/design/dist
